# Configure which "root" the website going to live under
site_root := /

# Add additional thing to preprocessor
preprocess_flags :=

# Set absolute URL referring to site's root including hostname and port
site_host_root := http://localhost:8080/
